# Project State (source of truth — re-read every step)

> Every command reads this on start. On conflict with context memory, this file wins.
> **Keep it at 100 lines or fewer.** If exceeded, run `/compact` to move history and evidence into `PROJECT_LOG.md` (read only by the commands that need it).
> This file is operational — keep it in English. Korean stays in `PROJECT_LOG.md` and in user-facing output.

- **Version:** v0
- **Updated:**
- **Harness version:** harness v8.7
- **Goal:**
- **Current step:** (not started)

## Invariants (cell state — never delete arbitrarily)
- **Applicable regulations / licenses / supervisory rules:** `<undetermined — halt until fixed>`
- **Forbidden automated actions:** `<e.g. auto-trading/transfers, e-signature, external transmission, production changes>`
- **Data classification & handling:** `<public / internal / confidential / personal / trade-secret + masking method>`
- **Compliance status:**

## Approvals
| Approval point | Status | Time |
|---|---|---|
| Scope & regulations fixed (design approval) | pending | |
| Build start | pending | |
| Record & publication (handoff approval) | pending | |

## Completion Verdict
> `/verify` writes this. The prompts generated by `/handoff` quote this value.

- **Verdict:** none (`complete / in progress / needs verification / conditionally complete / release held / user decision required`)
- **Time:**
- **Basis:** verification results section in `PROJECT_LOG.md`

## Active Design Decisions
> When a decision is reversed, delete it here and move it down to `PROJECT_LOG.md`.
> **On reversal, also record the scope of specs invalidated.** Omit this and shell implementations survive.

- (none)

## Open Labels (blocks completion)
> Any remaining label blocks completion and publication. `tools/gate.py` checks this.

- `[미검증]`:
- `[확인 필요]`:

## Open Feedback
> Numbers point to the feedback ledger in `PROJECT_LOG.md`. When resolved, remove from this list only (never from the ledger).

- (none)

## Next Action · Awaiting Approval
- environment check, then `/design`

## Halt Reason
> Fill this in whenever the project stops. Never halt leaving it blank.
> e.g. token/context budget exhausted / requirement withdrawn / technically infeasible / priority change

- **Halted:** no
- **Halted at step:**
- **Reason:**
