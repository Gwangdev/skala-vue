# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

.eslintcache

# Cypress
/cypress/videos/
/cypress/screenshots/

# Vitest
__screenshots__/

# Vite
*.timestamp-*-*.mjs

# Environment variables — 실제 키가 든 .env는 제외, 모드 라벨만 든 파일은 예외로 추적
.env
.env.*
!.env.example
!.env.staging
!.env.production
__pycache__/
.claude/settings.local.json

# 문법 연습 스크래치 (로컬 전용 — 제출물 아님, 실습9에서 저장소 추적 해제)
src/components/practices/

# 강의자료 및 과제 가이드 (저작물 — 저장소 공개 대상 아님)
guide/

# prompt-maker-harness (로컬 전용 — 과제 제출물이 아님)
# 경로 참조가 루트 기준으로 고정되어 있고 harness-init이 항상 루트로 rsync하므로
# 폴더를 옮기지 않고 추적에서만 제외한다.
.claude/
CLAUDE.md
HARNESS.md
USER_GUIDE.md
INSIGHTS.md
PROJECT_STATE.md
PROJECT_LOG.md
SPEC.yaml
reference/
tools/

# 필름 UI 샌드박스 (로컬 전용 — 제출물 아님)
sandbox.html
src/sandbox/

# Codex의 superpowers 스킬이 생성하는 작업 파일 (로컬 전용 — 제출물 아님)
docs/superpowers/
